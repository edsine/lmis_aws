'use strict';

/**
 * statistic-summary service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::statistic-summary.statistic-summary');
