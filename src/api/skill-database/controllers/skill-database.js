'use strict';

/**
 * skill-database controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::skill-database.skill-database');
